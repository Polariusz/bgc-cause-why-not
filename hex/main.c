#include <stdio.h>

int main(void)
{
    int binary = 0b11110001;
    printf("binary: %b\n", binary);
    printf("octal:  %o\n", binary);
    printf("decima: %d\n", binary);
    printf("hex:    %x\n", binary);
    return 0;
}
